export default class Autocomplete {

  constructor(dom, options = {}) {
    this.dom = dom
    this.options = options
    this.init()
  }

  init() {
    this.addEvents()
  }

  addEvents() {
  }

  destroy() {
  }
}
